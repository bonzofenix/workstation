#!/usr/bin/env bash

source ~/workstation/bin/common.sh

function delete-branch() {
  git fetch -a
  local branch_name=$(git branch -l --format='%(refname:short)' | gum choose)
  gum confirm "Delete branch $branch_name?" || return 1
  git branch -d $branch_name
}

delete-branch
